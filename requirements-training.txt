# Optional: install for runnable LoRA / SFT / quantization
# pip install -r requirements-training.txt
torch>=2.0.0
transformers>=4.36.0
peft>=0.7.0
datasets>=2.16.0
accelerate>=0.25.0
# 4-bit quantization (GPU); omit if CPU-only
# bitsandbytes>=0.41.0
